name: require-labels
policies:
  - ../policies/require-labels.yaml
resources:
  - require-labels-resources.yaml
results:
  - policy: require-labels
    rule: check-labels
    resource: good-pod
    kind: Pod
    result: pass
  - policy: require-labels
    rule: check-labels
    resource: bad-pod
    kind: Pod
    result: fail
