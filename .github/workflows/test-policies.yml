name: Test Kyverno Policies

on:
  push:
    paths:
      - 'policies/**'
      - 'tests/**'
  pull_request:
    paths:
      - 'policies/**'
      - 'tests/**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install kyverno CLI
        run: |
          curl -LO https://github.com/kyverno/kyverno/releases/download/v1.14.0/kyverno-cli_v1.14.0_linux_x86_64.tar.gz
          tar -xzf kyverno-cli_v1.14.0_linux_x86_64.tar.gz
          sudo mv kyverno /usr/local/bin/

      - name: Run kyverno tests
        run: |
          cd tests
          for f in *-test.yaml; do
            kyverno test . -f "$f" --detailed-results || exit 1
          done